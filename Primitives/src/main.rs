fn main() {
    // Vars can be type annotated
    let logical: bool = true;

    // regular
    let a_float: f64 = 1.0;
    // suffix annotation
    let an_int = 5i32;

    // use defaults
    let default_float = 3.0; // f64
    let default_int = 7; // i32
    let mut mutable = 12; // vars do not need to be mutable

    // the type of variable can be changed
    // mutable = true;
}
